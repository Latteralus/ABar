export const REAL_ESTATE_CONFIG = {
  /** How many of a background property's own recent real days feed the trailing-average revenue/COGS estimate. */
  backgroundEstimateTrailingDays: 7,
  /** Resale value of owned equipment/attractions when a property is closed out, as a fraction of original purchase price. */
  equipmentAttractionResalePercent: 0.5,
  /** Resale value of a bought (not leased) property when closed out, as a fraction of its catalog purchase price. */
  propertyResalePercent: 0.65,
};
